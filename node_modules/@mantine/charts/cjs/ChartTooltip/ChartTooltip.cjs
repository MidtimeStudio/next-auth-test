'use client';
'use strict';

var React = require('react');
var core = require('@mantine/core');
var ChartTooltip_module = require('./ChartTooltip.module.css.cjs');

function getFilteredChartTooltipPayload(payload) {
  return payload.filter((item) => item.fill !== "none");
}
const defaultProps = {};
const ChartTooltip = core.factory((_props, ref) => {
  const props = core.useProps("ChartTooltip", defaultProps, _props);
  const { classNames, className, style, styles, unstyled, vars, payload, label, unit, ...others } = props;
  const getStyles = core.useStyles({
    name: "ChartTooltip",
    classes: ChartTooltip_module,
    props,
    className,
    style,
    classNames,
    styles,
    unstyled
  });
  if (!payload) {
    return null;
  }
  const filteredPayload = getFilteredChartTooltipPayload(payload);
  const items = filteredPayload.map((item) => /* @__PURE__ */ React.createElement("div", { key: item.name, ...getStyles("tooltipItem") }, /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemBody") }, /* @__PURE__ */ React.createElement(
    core.ColorSwatch,
    {
      color: item.color,
      size: 12,
      ...getStyles("tooltipItemColor"),
      withShadow: false
    }
  ), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemName") }, item.name)), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemData") }, item.payload[item.dataKey], unit)));
  return /* @__PURE__ */ React.createElement(core.Box, { ...getStyles("tooltip"), ref, ...others }, label && /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipLabel") }, label), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipBody") }, items));
});
ChartTooltip.displayName = "@mantine/charts/ChartTooltip";

exports.ChartTooltip = ChartTooltip;
exports.getFilteredChartTooltipPayload = getFilteredChartTooltipPayload;
//# sourceMappingURL=ChartTooltip.cjs.map
