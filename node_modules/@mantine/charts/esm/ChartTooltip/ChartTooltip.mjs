'use client';
import React from 'react';
import { factory, useProps, useStyles, ColorSwatch, Box } from '@mantine/core';
import classes from './ChartTooltip.module.css.mjs';

function getFilteredChartTooltipPayload(payload) {
  return payload.filter((item) => item.fill !== "none");
}
const defaultProps = {};
const ChartTooltip = factory((_props, ref) => {
  const props = useProps("ChartTooltip", defaultProps, _props);
  const { classNames, className, style, styles, unstyled, vars, payload, label, unit, ...others } = props;
  const getStyles = useStyles({
    name: "ChartTooltip",
    classes,
    props,
    className,
    style,
    classNames,
    styles,
    unstyled
  });
  if (!payload) {
    return null;
  }
  const filteredPayload = getFilteredChartTooltipPayload(payload);
  const items = filteredPayload.map((item) => /* @__PURE__ */ React.createElement("div", { key: item.name, ...getStyles("tooltipItem") }, /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemBody") }, /* @__PURE__ */ React.createElement(
    ColorSwatch,
    {
      color: item.color,
      size: 12,
      ...getStyles("tooltipItemColor"),
      withShadow: false
    }
  ), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemName") }, item.name)), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipItemData") }, item.payload[item.dataKey], unit)));
  return /* @__PURE__ */ React.createElement(Box, { ...getStyles("tooltip"), ref, ...others }, label && /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipLabel") }, label), /* @__PURE__ */ React.createElement("div", { ...getStyles("tooltipBody") }, items));
});
ChartTooltip.displayName = "@mantine/charts/ChartTooltip";

export { ChartTooltip, getFilteredChartTooltipPayload };
//# sourceMappingURL=ChartTooltip.mjs.map
